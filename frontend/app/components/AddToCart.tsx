'use client';

const AddToCart = () => {
  return (
    <button
      className='border p-2 cursor-pointer'
      onClick={() => console.log('Hola')}
    >
      Add to Cart
    </button>
  );
};

export default AddToCart;
